import { View, Text, FlatList, StyleSheet } from "react-native";

function CarbsQuizScreen() {
  return (
    <View
      style={{
        flexDirection: "row",
        alignItems: "center",
        justifyContent: "space-between",
        padding: 10,
      }}
    >
      <Text>cab</Text>
    </View>
  );
}

export default CarbsQuizScreen;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
});
